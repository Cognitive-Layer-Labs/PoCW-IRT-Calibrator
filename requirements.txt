# IRT Training Pipeline — Dependențe
# Instalare: pip install -r requirements.txt

# ── Dataset ──────────────────────────────────────────────────
datasets>=2.18.0          # HuggingFace datasets (MMLU etc.)
huggingface-hub>=0.21.0

# ── Embedding ─────────────────────────────────────────────────
sentence-transformers>=2.7.0
torch>=2.2.0              # PyTorch (MPS pe Apple Silicon)

# ── IRT Fitting ───────────────────────────────────────────────
py-irt>=0.7.0             # Bayesian IRT 1PL/2PL/4PL via PyTorch
scipy>=1.12.0             # fallback MLE scipy

# ── ML ────────────────────────────────────────────────────────
xgboost>=2.0.0
scikit-learn>=1.4.0
joblib>=1.3.0

# ── Data ──────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
pyarrow>=15.0.0           # pentru .parquet

# ── HTTP / Async ──────────────────────────────────────────────
httpx>=0.27.0             # async HTTP pentru Ollama API

# ── Progress / Utils ──────────────────────────────────────────
tqdm>=4.66.0
matplotlib>=3.8.0
